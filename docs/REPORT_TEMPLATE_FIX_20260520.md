# 报告模板注入修复记录 2026-05-20

## 修复范围

- `core/report/template_injector.py`
  - 图5.1 固定使用 `SHITI` 全模型图。
  - 图5.2 固定使用 `TBMODEL/TUOBI_MODEL` 托臂模型图，禁止复用图5.1。
  - 表3-1 不再受模板初始行数限制，按 `spectrum_points.json` 的谱点数量动态扩展。
  - 水平方向反应谱按 X/Y 曲线取包络，不再只取第一条水平曲线。
  - 附录C按方钢外边长独立分支：
    - `<= 120 mm`：附录C为托臂应力云图，要求 `TB*/TD*` 8张图；
    - `> 120 mm`：附录C为焊缝评定原理，不输出托臂云图。
  - 附录C改写只匹配正文标题，不匹配目录项，避免误删正文。

## 验证样例

- `18185NI-LXSJ4142`
  - 模板：非钢平台模板。
  - 表3-1：57行谱点。
  - 图5.1、图5.2、附录A、附录B全部注入成功。
  - 附录C：焊缝评定原理。
  - `template_report_audit.json.status = pass`。

- `18185NI-LXSJ4123`
  - 模板：钢平台模板。
  - 表3-1：114行谱点，模板自动扩展。
  - 附录C：托臂应力图，8张 `TB*/TD*` 图全部注入成功。
  - 当前历史 job 缺少 `TBMODEL`，所以图5.2仍记录 warning；新计算链路必须导出 `TBMODEL` 后才能发布正式报告。

## 防回归测试

新增/更新 `tests/unit/test_template_report_injector.py`：

- 表3-1动态扩展并填充超过模板原始44行的数据。
- 图5.2缺少专用托臂模型图时必须 warning，不能复用图5.1。
- 附录C托臂云图分支必须注入8张图，包含两张弯曲图。
- 大截面方钢必须切换到焊缝评定原理分支，不保留托臂云图章节。
