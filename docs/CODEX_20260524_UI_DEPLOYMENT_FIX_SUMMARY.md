# 2026-05-24 网页、部署和历史验证修复摘要

## 1. Skills 处理

- 已删除本机 Codex skills：`codegraph`、`code-graph`。
- 已安装 `forrestchang/andrej-karpathy-skills` 中的 `karpathy-guidelines`。
- 本轮网页布局按 `ui-ux-pro-max` 的工程工作台原则调整：降低拥挤度、扩大模型核查区、隐藏非主流程部署配置面板、保留后台质控入口。

## 2. 历史批量验证状态

已重新执行后验报告验证：

- 报告样例数：47
- 通过：8
- 已记录为历史报告/源文件冲突：15
- 仍存在超过 1% 差异：24
- 阻塞/脚本错误：0

这 24 个不是当前运行失败，也不能硬改数据伪装通过。它们保留为后验验证问题，继续按模态、支架应力、焊缝、基础载荷、连接螺栓、报告历史源冲突逐项审查。

本轮已修复一个实际运行链路问题：1916 新提资中 `LL` 厂房与反应谱工作簿 `LX廊道区` 的别名不一致，已加入配置化别名，验证样例运行状态为 `pass`。

## 3. 图 5 和报告注入

报告注入逻辑继续要求：

- 图 5.1 使用 `SHITI`，代表 S2 支架整体有限元模型。
- 图 5.2 使用 `TBMODEL`，代表托臂有限元模型。
- 不允许用同一张图替代两张模型图。
- 模型图由 ANSYS 图片导出链路生成，报告注入只替换图片和表格，不自由排版。

相关测试已覆盖图 5.1/图 5.2 不混用、模板注入字段和结果有效性门禁。

## 4. 网页端调整

- 页面名称统一为“电缆桥架力学分析一体化平台”。
- 主页面保留电缆桥架计算主流程，后台 AI 质控隐藏为状态入口，不再占用大块页面。
- 模型核查区和三份命令流预览区扩大，便于科室审核建模、计算和提取命令。
- 后台质控继续自动巡检 UNKNOWN 节点、全零载荷、缺图、报告注入风险；正式结论仍以 ANSYS、Excel 和确定性公式为准。

## 5. 单位内网部署兜底

新增：

- `CHECK_NETWORK_ACCESS.cmd`
- `网络访问自检.cmd`
- `scripts/check_intranet_access.ps1`
- `docs/INTRANET_FIREWALL_FALLBACK.md`

若部署电脑本机能打开 `127.0.0.1:8000`，但其他电脑打不开 `10.x.x.x:8000`，优先判断为 Windows 防火墙、安全软件、VLAN 路由或单位网络策略问题，而不是平台服务启动失败。

部署包仍提供免 PowerShell 入口 `START_NO_POWERSHELL.cmd`，用于单位策略禁用 `.ps1` 的场景。

## 6. 验证

- `pytest -q`：通过。
- 硬编码扫描 `core apps templates`：无 `1818 / 7.5m / NB` 命中。
- `source_materials`：无修改。
- 本机网页：
  - `http://127.0.0.1:8000/`：200
  - `http://127.0.0.1:8000/ai-tools`：200

## 7. 最新部署文件

- 完整离线包：`C:\Users\duxy\Desktop\duxyb\CableTrayAI_intranet_release_20260524_095849.zip`
- 增量更新包：`C:\Users\duxy\Desktop\duxyb-up\CableTrayAI_update_20260524_100303.zip`

