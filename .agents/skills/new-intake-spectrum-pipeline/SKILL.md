---
name: new-intake-spectrum-pipeline
description: Build jobs from new intake files and confirmed spectrum workbook configurations.
---

# Requirements

- Intake creates `input.json` and `job_creation_audit.json`.
- Spectrum configuration must be confirmed before real ANSYS.
- Spectrum parsing supports exact elevation and interpolation.
- New projects are configured, not hardcoded.
